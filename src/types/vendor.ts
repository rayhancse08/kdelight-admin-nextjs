export type Vendor = {
  id: number;
  name: string;
  phone_number: string;
  email: string;
  address: string;
  created: string;
  updated: string;
};

export type VendorFormData = {
  name: string;
  phone_number: string;
  email: string;
  address: string;
};