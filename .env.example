# Django REST API — browser calls this directly (no Next.js proxy)
NEXT_PUBLIC_API_URL=https://kdelight.info

# Django must allow CORS from your frontend origin, e.g.:
# CORS_ALLOWED_ORIGINS = ["http://localhost:3000", "https://your-app.vercel.app"]
